var mongoose = require('mongoose');

//creating a new user
var users = new mongoose.Schema({
  name: String,
  surname : String,
  email : String,
  userName : String,
  passWord : String,
  token : String
});

module.exports = mongoose.model('users', users);

var mongoose = require('mongoose');

//creating a row in the File middle
var middle = new mongoose.Schema({
  token : String,
  latitude : Number,
  longidude : Number
});

module.exports = mongoose.model('middle', middle);

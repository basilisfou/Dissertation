var mongoose = require('mongoose');

//creating a row in the File locations
var locations = new mongoose.Schema({
  token : String,
  latitude : Number,
  longidude : Number,
  date : String
});

module.exports = mongoose.model('locations', locations);
